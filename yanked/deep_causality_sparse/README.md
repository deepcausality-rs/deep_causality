# deep_causality_sparse

> ## This crate is retired
>
> Moved to `yanked/` and removed from the workspace. **0.2.5 is the last release on
> crates.io.** The published versions stay up and keep working for existing dependents;
> the crate is not yanked from the registry, and no further release is planned.
>
> `deep_causality_sparse` receives no further development. Its contents live in
> **[`deep_causality_linear`](../../deep_causality_linear)**, 
