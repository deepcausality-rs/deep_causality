---
title: Pick a solver family
task: You have a flow problem and need to know which of the three families to point at it.
summary: Dispatch happens at compile time, so choosing is a matter of handing march a different config type. The harder question is which family your problem actually belongs to.
order: 4
verbs:
  - march
  - MarchDispatch
  - FloatType
worked:
  - label: VIV resonance margin
    href: /examples/viv-resonance-margin/
  - label: Nozzle operating map
    href: /examples/nozzle-operating-map/
---

```rust
// One scalar type, three solver families, one language.
type FloatType = f64;   // swap for Float106 without touching the physics

// Incompressible, fixed mesh -> DEC
let cavity = CfdFlow::march(&dec_config).run_owned()?;

// Compressible layer, 2^L grid -> QTT marcher
let layer  = CfdFlow::march(&compressible_config).from_field(seed).run()?;

// Stagnation line, no grid at all -> fitted closure
let post   = FittedNormalShock::post_shock(mach, gamma, t_inf);
```

`MarchDispatch` selects the pipeline from the config type at compile time. There is
no runtime solver registry and no dynamic dispatch.

## Choosing

| Your problem | Family | Evidence |
|---|---|---|
| Incompressible, fixed mesh, wall-bounded | DEC Navier–Stokes | [Ghia Re 1000](/validation/#dec-cavity) |
| Compressible layer where a 2^L grid must stay cheap | QTT marcher | [Sod, L1 ≤ 0.027](/validation/#qtt-sod) |
| Stagnation line, relaxation, no grid needed | Fitted closures | [RAM-C II anchor](/validation/#qtt-ramc) |
| Curved shock on Cartesian axes at high resolution | None — see boundaries | [χ ~ √side](/boundaries/#sqrt-side) |

The last row is the one to read carefully. QTT compression is conditional on
coordinate alignment, not on smoothness. A curved shock captured on Cartesian axes
costs *more* than a dense grid, and in 3-D the bond dimension grows as roughly the
square root of the side length. If that describes your problem, the answer is to
align the coordinate — or to use a different family.

## The DEC type-state

Velocity lives as an edge 1-form and each step marches the Leray-projected rate, so
the field is divergence-free at every step. The `SolenoidalField` type-state makes
time-stepping an unprojected field a compile error rather than a slowly diverging
run.

Measured divergence residual on the wake case: **3.33e-15**, against a 1e-6
tolerance. That is not a solve converging; it is the discretization being exact by
construction and the number reporting round-off.

## Precision is a parameter, not a rebuild

Every example carries one alias:

```rust
pub type FloatType = f64;
```

The physics is written against the scalar bound, so changing that line
re-instantiates the whole computation. `f32`, `f64` and `Float106` all work — with
the caveat that `f32` is not merely less accurate. In the corridor it fails at step
one, because a term in the ionization kernel evaluates to 4.4e-67 and underflows the
exponent range.

See [forecast horizon under precision](/examples/turbulence-flow/) for what the
choice actually buys: roughly a doubling of usable forecast horizon per nine orders
of magnitude of epsilon.
