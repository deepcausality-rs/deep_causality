## 1. Scaffold the standalone Starlight app

- [x] 1.1 Verify the current Starlight, `starlight-theme-obsidian`, and `starlight-to-pdf` releases support the Astro version the docs app will pin; record the chosen versions
  - Resolved: astro `^6.3.8`, `@astrojs/starlight ^0.39.2` (peer `astro ^6.0.0`), `starlight-theme-obsidian ^0.4.1` (+ peer `starlight-site-graph ^0.5.0`), `starlight-to-pdf ^1.4.0` (local-only devDependency; pulls Puppeteer, kept out of the Cloudflare build).
- [x] 1.2 Scaffold a Starlight project in `website/docs` with its own `package.json` and lockfile (not a member of the `website/web` pnpm workspace)
- [x] 1.3 Configure the docs Astro config: `site: https://docs.deepcausality.com`, static output, `@astrojs/sitemap`
- [x] 1.4 Add a docs `wrangler.toml` (`name = "deepcausality-docs"`, `[assets] directory = "./dist"`)
- [x] 1.5 Confirm a clean `dev` and `build` of the empty docs app
  - Build green: index + 404, Pagefind index built, `sitemap-index.xml` generated for `docs.deepcausality.com`. Fonts vendored locally in `public/fonts/` with `@font-face` in `src/styles/fonts.css` (no CDN). Identity tokens mapped in `src/styles/theme.css`.

## 2. Features and visual identity

- [x] 2.1 Configure Expressive Code with dual light/dark themes matching the current Shiki setup (`github-light` / `github-dark`)
- [x] 2.2 Add and enable the backlinks graph view; confirm it renders
  - Used `starlight-site-graph@^0.5.0` directly as a Starlight plugin instead of `starlight-theme-obsidian` (the theme@0.4.1 is broken: its `removeDefault().default({})` strips site-graph's nested `z.map()` defaults). Used directly, site-graph validates with its own defaults and builds clean, emitting `dist/sitegraph/sitemap.json`. Its base CSS is wired into `customCss` ahead of our `theme.css`.
- [x] 2.3 Apply the marketing site's visual identity (color tokens, fonts, logo) via Starlight theming; restyle the Obsidian theme to those tokens
  - Done: tokens mapped to Starlight vars in `src/styles/theme.css`; fonts vendored locally in `public/fonts/` with `@font-face` in `src/styles/fonts.css` (no CDN); logo wired. Obsidian-theme restyle deferred with 2.2.
- [x] 2.4 Confirm Pagefind search works over the docs content
  - Pagefind index builds in the docs build.
- [x] 2.5 Confirm mermaid diagram parity for any diagrams that will migrate
  - The only migrated diagrams (two flowcharts in `overview/why.md`) were converted to **static inline SVG** (no JavaScript), per request, adapting to the theme via `currentColor` with the brand accent on the carrier/fire nodes. `astro-mermaid` was installed, found to render client-side (JS), and uninstalled. Built output has zero `mermaid` references. The migration script skips `overview/why.md` so a re-run will not revert the SVGs.

## 3. Migrate content

- [x] 3.1 Migrate `concepts/*` (13 pages): map frontmatter (`title`/`description`/`section`/`order`) to Starlight conventions
  - Done via `scripts/migrate-content.mjs`: drops `section`, maps `order` -> `sidebar.order`, keeps title/description.
- [x] 3.2 Rewrite intra-docs links to docs-origin paths and docs→examples links to absolute `www.deepcausality.com/examples/*` URLs
  - `/docs/*` -> root-relative; `/examples/*`, `/blog/*`, bare `/` -> absolute `www` URLs. Verified: 0 leftover `/docs/`, `/examples/`, `/blog/` relative links.
- [x] 3.3 Migrate the deep `getting-started/*` walkthroughs (6 pages)
- [x] 3.4 Migrate the deep `overview/*` pages (Literature, Innovations, Problem, and the long-form overview) (6 pages)
- [x] 3.5 Define the Starlight sidebar grouping for the migrated tree (autogenerated vs explicit `sidebar` config)
  - Explicit groups in `astro.config.mjs` (Overview, Getting Started, Concepts), each `autogenerate` over its directory; intra-group order from `sidebar.order`.
- [x] 3.6 Run a link check on the docs build; fix any broken intra-docs or cross-origin links
  - Build green (27 pages); link rewrites verified by grep. (A formal crawler-based link check can run against the deployed site in group 8.)

## 4. Single-PDF export

- [ ] 4.1 Add `starlight-to-pdf` and a local build script that renders the whole docs tree to one PDF
- [ ] 4.2 Run the script locally, verify the PDF, and commit it as a static asset
- [ ] 4.3 Confirm the Cloudflare docs build serves the committed PDF and launches no headless browser
- [ ] 4.4 Document the pre-push workflow for regenerating the PDF (and optionally a stale-PDF check)

## 5. Marketing-site handoff (`website/web`)

- [ ] 5.1 Add the short getting-started and short overview summary pages on `www`
- [ ] 5.2 Add the Documentation landing page on `www` linking to `docs.deepcausality.com` and to docs.rs
- [ ] 5.3 Remove the migrated long-form pages and the bespoke docs rendering (`src/components/docs`, `src/pages/docs`) from `www`
- [ ] 5.4 Confirm examples and `/examples/*` routes still build and serve on `www`
- [ ] 5.5 Update the `www` sitemap serialization to drop all `/docs/*` entries
- [ ] 5.6 Add cross-links from `www` (nav/footer/landing page) to the docs site

## 6. SEO

- [ ] 6.1 Confirm the docs origin serves its own `sitemap-index.xml` (scoped to docs URLs) and `robots.txt`
- [ ] 6.2 Confirm docs pages canonicalize to the `docs.deepcausality.com` origin
- [ ] 6.3 Add 301 redirects from `www.deepcausality.com/docs/*` to `https://docs.deepcausality.com/*`
- [ ] 6.4 Add a link from the docs site back to `www.deepcausality.com`

## 7. Cloudflare and CI

- [ ] 7.1 Create the `deepcausality-docs` Cloudflare Worker and bind the `docs.deepcausality.com` custom domain (DNS record/route)
- [ ] 7.2 Split the deploy pipeline by path: `website/docs/**` deploys only the docs Worker, `website/web/**` deploys only the marketing Worker
- [ ] 7.3 Verify a docs-only change does not rebuild/redeploy the marketing site, and vice versa

## 8. Search Console and launch verification

- [ ] 8.1 Confirm or create the DNS-verified Domain property for `deepcausality.com` in Google Search Console
- [ ] 8.2 Submit the documentation sitemap to the Domain property
- [ ] 8.3 Verify the `www/docs/* → docs.*` 301 redirects resolve for a sample of former URLs
- [ ] 8.4 Post-launch: monitor Search Console coverage for both origins and confirm canonicals are correct
