## 1. Establish the CI baseline (before touching any gate)

Per design D4 and the migration plan: wire CI against the suite as it stands, so later gate failures are
attributable to the gate change rather than to the wiring.

- [x] 1.1 Record the current pass/fail state of all 13 verification harnesses by running each in release and capturing exit codes into the change's working notes (baseline for attribution) — see `baseline.md`; all 13 exit 0 before the change
- [x] 1.2 Add a `verification` job running the 10 fast harnesses (`mms_taylor_green`, `dec_graded_mms`, `dec_taylor_green_re1600`, `qtt_taylor_green`, `qtt_cylinder`, `qtt_park2t_blackout`, `qtt_sod`, `qtt_ramc_stagline`, `qtt_blunt_body_2d`, `qtt_reentry_3d`) in `--release`, failing the build on any non-zero exit — **deviation:** landed in a dedicated `.github/workflows/cfd_verification.yml` rather than as a job inside `run_tests.yml` (see 1.3)
- [x] 1.3 Add a `schedule`-triggered run of `dec_cylinder_verification`, `dec_cylinder_wake_verification` and `dec_lid_cavity_re1000_verification`, reporting separately from the PR suite — **deviation:** implemented as a `slow` job in the same `cfd_verification.yml` under `on: [pull_request, schedule, workflow_dispatch]` with `if:` guards, instead of a separate `verification_nightly.yml`. One file keeps the two harness lists and the completeness check (1.4) in a single place, which is what the check guards; the design's reason for a separate file was only to avoid putting `schedule` on `run_tests.yml`, and a dedicated workflow already achieves that.
- [x] 1.4 Add a completeness check asserting the union of the two workflow harness lists equals the verification `[[example]]` targets declared in `deep_causality_cfd/Cargo.toml`, so a new harness cannot escape CI — `verification-suite-complete` job; dry-run locally passes at 13/13 and correctly fails when a harness is dropped from the lists
- [x] 1.5 Verify the fast job adds roughly 20 s to PR wall-clock and the nightly roughly 12 min; record actuals — fast measured **19.9 s** uncontended (excludes build); slow ~12 min at documented rates. CI-runner actuals pending first scheduled run.

## 2. Give `dec_cylinder_verification` a real gate (blocker B-3)

- [x] 2.1 Replace the solver-error `break` with a reported failure and `std::process::exit(1)`, so a truncated march can never report `St` and `C_d`
- [x] 2.2 Add gates on `St` and `C_d` against the cited Williamson / Dröge–Verstappen bands, printing the reference values and the grid resolution next to the measured values — four gates: shedding detected, drag measured, `St` band, `C_d` band. Reference values and the % offset print beside each measurement, with the grid resolution in the block header.
- [x] 2.3 Label each new bound `reference` or `tripwire` per design D1/D2; at the affordable 8 cells/D default a `tripwire` label is expected, with the reference band still printed — all four are `tripwire`, as anticipated: measured St 0.1710 vs Williamson 0.164 (+4.3 %) and C_d 1.345 vs the 1.24–1.33 band (+1.2 % over top) both sit **outside** the published bands at this resolution, so gating against them would fail a working solver. Bands are pinned ~±10 % around the measured default; width set by cross-platform FP sensitivity of a 1500-step nonlinear march, and flagged provisional pending CI data. The `St`/`C_d` bands apply only at the reference `Re = 100` — a Reynolds-ladder run prints `[SKIP]` rather than passing silently against a band that does not describe it.
- [x] 2.4 Record each gate's breaking condition as a comment (design D3)
- [x] 2.5 Confirm the harness exits non-zero when a gate is forced to fail, and that CI reports it — demonstrated with `STEPS=120` (too short to shed): `[FAIL] no shedding detected`, **exit 1**. The same run on the pre-change harness exited 0. Full-length run passes all four gates, exit 0.

## 3. Repair the gates that cannot fail

One harness per commit, each carrying its gate change, breaking-condition comment, and any spec delta.
Preference order per design D5: make the predicate read the quantity under test → replace with an
independent reference → delete and correct the advertised gate count.

- [x] 3.1 `qtt_taylor_green_verification`: compute the convection amplitude from the solver output `cs[]` rather than from the analytic reference, and gate the computed-to-analytic amplitude ratio — measured 0.497 vs analytic 0.500 (ratio 0.994, band [0.9, 1.1]); falsifiability demonstrated by zeroing the amplitude → FAIL, exit 1 (the old `conv_amp <= 0.0` form could never fail)
- [x] 3.2 `qtt_park2t_blackout` gate (ii): replace the transcription of `ler_step`'s body with a comparison against a converged sub-stepped integration of `dx/dt = (x_eq − x)/τ` within a documented tolerance — 10⁶ forward-Euler sub-steps, tolerance 1e-6 derived from the reference's own `a²/(2N) = 4.5e-8` truncation error (~30× margin), not pinned from the measurement. Falsifiability demonstrated: flipping the `ler_step` exponent sign makes (ii) FAIL, exit 1.
- [x] 3.3 `qtt_park2t_blackout` gate (iv): remove the `tau = 0` conjunct (it asserts an explicit early `return x_eq`), retaining the falsifiable conjuncts — `grounded` (Arrhenius monotone in T) and `lagged < α_eq` both survive and both can fail. Gate labels (ii) and (iv) updated to describe what they now check.
- [x] 3.4 Land the `park2t-blackout-validation` spec delta with 3.2 and 3.3 — the existing requirement mandates the tautology, so the code fix alone is insufficient — delta authored at `specs/park2t-blackout-validation/spec.md`; the implementation now matches it
- [x] 3.5 `qtt_rank_dynamic` gate G2: replace `b_peak < b0` (unreachable, since `peak` is initialised to `init` and only ever `max`'d) with a gate asserting the peak exceeds the smooth-encode floor by a stated margin — now requires strict rank growth under marching (`b_peak > b0`); measured init 4 → peak 8, a factor-of-two margin. Falsifiability demonstrated: forcing `b_peak == b0` (a no-op marcher) fails G2, exit 1.
- [x] 3.6 Weather example "(0) table integrity": populate `errored` from a real per-row outcome, or delete the gate and correct the advertised gate count — `errored` was structurally dead (an error short-circuits `march_for` before a row exists, so the flag could never be true). Field removed; gate re-scoped to row-count-vs-declared-cases plus finiteness of `ne_max`/`dwell_s`/`q_max`, both of which can fail.
- [x] 3.7 Corridor example "(4f) fine sweep refines the coarse winner": the fine candidate list always contains the coarse winner, so the inequality holds identically — re-scope to a comparison that can fail, or delete — re-scoped to two falsifiable checks: the fine sweep brackets the coarse winner on both sides, and the branches flew distinct worlds (non-zero miss spread). Measured: brackets [12.5, 17.5]° around the 15° winner, spread 3.751 m over 11 branches. The improvement is still printed as detail.
- [x] 3.8 Compressible MMS `continuity_error` gate (`src/types/flow/mms.rs`): stop feeding literal zeros, or remove the metric if no informative input exists — `div_u` is now the trace of the manufactured Jacobian rather than `R::zero()`, so the zero residual is a *result* of the field being divergence-free. Falsifiability demonstrated: perturbing the Jacobian trace by 0.25 fails `compressible_regime_pins_continuity`. (`∇ρ` stays zero — constant density is part of the manufactured solution, not an assumption about the kernel.)
- [x] 3.9 Energy-budget gate (`tests/solvers/dec/energy_budget_tests.rs`) — **AUDIT FINDING REFUTED, no change made.** `energy_budget` computes `convective = −⟨u, Cu⟩_M` from the *actual* operator output, not from a formula that is zero for any `C`. Tested by flipping the adjoint sign in `fill_convective_skew_fused` so the operator is no longer skew: the assert fails with convective power 0.0338 (vs ~0). The gate is a genuine guard that the shipped convective operator remains skew-symmetric. The auditor conflated "zero given the correct operator" with "zero regardless of the operator" — only the latter would be a tautology. Recorded for the audit report's overturned-findings section.
- [x] 3.10 For every gate touched in this group, add the breaking-condition comment and, where cheap, a unit test asserting the predicate returns false for that input (design D3) — BREAKING CONDITION comments added at every repaired gate; each was demonstrated live by fault injection rather than asserted (3.1, 3.2, 3.5, 3.8 each shown failing, then reverted)

## 4. Re-found the QTT cylinder gate set

Design D6 applies throughout: gate the trend and permit an explicit non-convergence outcome rather than
forcing a pass/fail on an absolute value.

- [x] 4.1 Add a smoothing-width ladder (0.5, 1, 2, 3, 4 cells) reporting `C_d` at each width and gating the trend — measured `C_d` 7.70, 12.33, 23.76, 35.81, 47.27: a **6.1×** span driven by a purely numerical parameter. Judged `NOT CONVERGING` (monotone, far outside tolerance). Gates.
- [x] 4.2 Add an η ladder (0.128 → 0.008) reporting `C_d` and interior slip at each η, gating whether the sequence converges and reporting non-convergence explicitly when it does not — measured `C_d` 17.39, 24.02, 26.25, 23.76, 21.40: rises, peaks, then falls. Judged `NOT CONVERGING` via the direction-reversal branch of `LadderOutcome` — no η → 0 limit, so the Angot convergence precondition is not demonstrated. Gates.
- [x] 4.3 Tighten `CONVERGENCE_BOUND` from 0.10 to the scale of the measured successive difference (~1.89e-11), so a solver that had not saturated in bond would fail — set to `1e-6`: ~6 orders above the measured 8e-13 relative change (absorbing cross-platform FP) instead of the previous eleven. Still passes on the committed ladder.
- [x] 4.4 Print the Reynolds number of both sides of the DEC cross-reference (QTT `Re = 37.7` vs DEC `Re = 100`) and mark the cross-reference disclaimed, not a gate — cross-reference line already prints `(Re 100)` and `disclaimed`; `DEC_CD_REF` is display-only and never reaches `verify`
- [x] 4.5 Correct `main.rs:19`'s "positive and O(1)" description to match what the code enforces (`0 < drag < DRAG_SANITY_MAX`), and reword the `DRAG_SANITY_MAX` doc comment which mislabels a blow-up guard as an O(1) bound — doc comment now states it is a positivity/NaN/divergence tripwire, not an O(1) claim, and records that the configuration produces `C_d ≈ 23.8`
- [x] 4.6 If no affordable η converges, record the non-convergence as the harness result and note it for Phase 2 rather than widening any bound — **no η in the ladder converges, and no bound was widened.** The harness exits 1. Per owner decision the gate is kept and `qtt_cylinder_verification` moved from the fast/PR list to the **nightly** list, so the finding stays loud and tracked without blocking merges (it also resolves the 7.4 s → 37 s runtime growth). Root cause and the Phase-2 routing are documented in `.github/workflows/cfd_verification.yml` under `KNOWN-FAILING`: the physical Brinkman layer `√(ην) = 0.144·dx` is ~7× thinner than a cell and the resolution criterion `η ≥ dx²/ν = 0.771` is violated 48×, so the grid resolves the mask smoothing skirt rather than the penalization layer. Routed to AUDIT-REPORT §9 Phase 2 item 10.

## 5. Classify every gate bound by evidence class

- [x] 5.1 Add the `[reference]` / `[tripwire]` marker to the printed gate line across all 13 harnesses (design D2 — label at the call site, not a side table) — **complete. 38 gate lines across the 9 PR harnesses, 0 unlabelled**, plus the 4 nightly harnesses. Foundation: `EvidenceClass` (`src/types/evidence_class.rs`) carried on `GateOutcome`, rendered by `Verdict`'s `Display`, with `GateSeq::gate` defaulting to `Tripwire` and `GateSeq::reference_gate` opting in — so every DSL-driven program labels automatically. Hand-rolled harnesses were converted from failure-only `eprintln!` to labelled `[PASS]/[FAIL] [class]` lines: `qtt_sod`, `qtt_ramc_stagline`, `dec_cylinder_verification`, `qtt_park2t_blackout`, `qtt_taylor_green_verification`, `qtt_cylinder_verification`, `dec_taylor_green_re1600_verification`, `dec_cylinder_wake_verification`, `dec_lid_cavity_re1000_verification`, `qtt_blunt_body_2d`, `qtt_reentry_3d`.
  **Two further B-3 instances found and fixed while sweeping:** `mms_taylor_green_verification` had only a setup-error `process::exit` and no gate on its measured residual, and `dec_graded_mms_verification` had **zero** `process::exit` calls at all — both reported numbers and exited 0 unconditionally, so a kernel or order regression would have printed the loss and still passed. Both now gate against their analytic references (`reference` class): MMS residuals vs a precision-scaled `1e4·ε` bound (measured 1.11e-16 / 6.66e-16 vs 2.22e-12), and the graded-mesh finest-pair observed order ≥ 1.9 against the theoretical 2 (measured 1.98–2.01 across both operators, both norms, all four grading amplitudes).
- [x] 5.2 Label the `qtt_ramc_stagline` ±0.70-decade band `tripwire` (its own gate text already says "pinned from the measurement") and ensure no prose describes it as agreement with RAM-C flight data — all 7 gates labelled; the ±0.70-decade band and the carried-arm band are `tripwire`, while the peak-`n_e`-vs-RAM-C-II comparison is `reference` (the anchor is external even though the band width is chosen — the distinction is stated at the call site)
- [x] 5.3 Label the lid-cavity RMSE bounds `tripwire` (they carry headroom from their pinning run), keeping the Ghia table values shown separately as the reference — all three `trend`-mode gates converted from failure-only `eprintln!` to labelled `[PASS]/[FAIL] [tripwire]` lines with detail; measured 17² 0.2369 vs pinned 0.32, 33² 0.1309 vs pinned 0.20, trend margin 0.04. The Ghia table values remain the reference the RMSE is computed against and are reported by the default mode.
- [x] 5.4 Sweep the remaining harnesses and label every numeric bound; for each `reference` bound record the citation at the definition site — done as part of 5.1. `reference`-class bounds and their citations: `qtt_sod` (exact Riemann solution — Sod 1978 / Toro ch. 4), `qtt_taylor_green` (closed-form TG decay and the −½sin2x convection form; theoretical 2nd order), `qtt_park2t_blackout` gate (ii) (independently sub-stepped ODE reference), `qtt_ramc_stagline` g2 (RAM-C II flight anchor), `mms_taylor_green` (closed-form du/dt and exp(−2νt)), `dec_graded_mms` (theoretical order 2), `qtt_cylinder` ladders (Angot, Bruneau & Fabrie 1999 η→0 limit). Everything else is `tripwire`, each with the reason recorded at the constant.
- [x] 5.5 Reconcile `deep_causality_cfd/verification/README.md` so every documented bound carries the same class as the code, and no tripwire is presented as external validation — added a **"Convention: every gate declares its evidence class"** section defining `[reference]` vs `[tripwire]` with a worked example and the unlabelled-defaults-to-tripwire rule; extended the exit-nonzero convention section to state that CI now executes the suite; and added the `qtt_cylinder_verification` known-failing block with its measured ladders and Phase-2 routing.
- [x] 5.6 Cross-check the crate README's verification claims against the relabelled table and correct any that describe a tripwire as reference agreement — corrected the **"Everything Self-Verifies"** section: it now states CI executes the suite, explains the two evidence classes, and replaces *"the plasma-blackout examples validate an uncalibrated finite-rate ionization network against RAM-C II flight data"* with an order-of-magnitude framing naming the ±0.70-decade band as pinned from the measurement. This closes the audit's inconsistency between the crate README's "validate against flight data" and the verification README's own order-of-magnitude caveat.

## 6. Regenerate the truncated lid-cavity baseline

- [x] 6.1 Re-run `dec_lid_cavity_re1000_verification` to completion and commit a baseline carrying the RMSE, the vortex-center table and the verdict line — regenerated at the default 65²/t=100 (~20 min). The baseline now runs to t=99.98 (14220/14223) and carries centerline RMSE **0.0617** and the full vortex table: primary (0.5312, 0.5625) vs Ghia (0.5313, 0.5625), bottom-right (0.8594, 0.1094) exactly Ghia's value, bottom-left (0.0781, 0.0781) vs (0.0859, 0.0781). Because the default mode has no gates by design (they live in the `trend` subcommand), the `trend` gate block is appended under a labelled separator so the artifact carries a verdict — 40 lines, 3 gate lines.
- [x] 6.2 Ensure the baseline header's grid and horizon match the configuration whose numbers the harness and `verification/README.md` report (design D7) — the baseline header is 65²/t=100 while the README summary row reported `33², t=40`. Fixed the README rather than the baseline: the summary row and the per-example section now report the **default** 65² configuration (RMSE 0.0617, primary vortex matching Ghia to 1e-4 in x and exactly in y), with a note that the coarse 33² numbers are the `trend` rung's pinned bounds, not the headline. Every other row in that table reports its default, so this removes an inconsistent basis that **understated** the solver by more than 2× on the RMSE (audit §3.5).
- [x] 6.3 Audit the other 11 committed baselines for truncation and regenerate any that do not reach their terminal summary — **first pass checked truncation only and found the lid cavity to be the sole truncated baseline.** Task 7.4's stricter spec check ("carries the harness's reported quantities **and** its verdict line") then found a wider gap: **6 of 12 baselines carried no verdict at all**, because their gate blocks go to stderr while the baseline captured stdout only, or because the baseline predated the gates added in this change. Regenerated with both streams: `mms_taylor_green` (2 gate lines), `dec_graded_mms` (8), `qtt_taylor_green` (6), `dec_taylor_green_re1600` (2), `dec_cylinder_wake` (2), plus the cavity's appended `trend` block (3). `dec_cylinder_verification` had no `baseline.txt` at all — it now has one (227 lines, exit 0, 4 gate lines). **All 13 harnesses now carry a baseline and every one carries a verdict.** A `baseline.txt` convention section was added to `verification/README.md` recording the stdout+stderr capture command, the complete-run requirement, and the rule that a failing baseline is committed as failing.

## 7. Verify the change against its specs

- [x] 7.1 For each repaired gate, demonstrate the documented breaking input makes it fail and the harness exits non-zero (spec: every shipped gate is falsifiable) — demonstrated live by fault injection, then reverted, for: TG convection amplitude (zero the field → FAIL/exit 1), park2t (ii) (flip the `ler_step` exponent sign → FAIL/exit 1), `qtt_rank_dynamic` G2 (force `b_peak == b0` → FAIL/exit 1), MMS continuity (perturb the Jacobian trace → test FAIL), `dec_cylinder` (STEPS=120, no shedding → FAIL/exit 1). Every other repaired gate carries a BREAKING CONDITION comment at its definition.
- [x] 7.2 Confirm every gate line in every harness prints an evidence class (spec: every gate bound declares its evidence class) — **38 gate lines across the 9 PR harnesses, 0 unlabelled** (measured by grepping `[PASS|FAIL] [reference|tripwire]` against `[PASS|FAIL]`). The 4 nightly harnesses are likewise labelled.
- [x] 7.3 Confirm CI fails the build when a harness exits non-zero, on both the PR and nightly cadences (spec: CI executes the verification suite) — ran the workflow's own shell loop locally: with a forced failure it accumulates the harness name and exits 1; with the real suite it exits 0. The `verification-suite-complete` job was likewise dry-run: passes at 13/13 and correctly fails when a harness is dropped from either list.
- [x] 7.4 Confirm every committed baseline reaches its terminal summary (spec: committed baseline artifacts are complete runs) — **12 / 12 baselines now carry both their reported quantities and a verdict line; 0 without.** This check is what surfaced the wider gap recorded under 6.3 (six baselines were stdout-only or predated their gates). `qtt_cylinder_verification`'s regenerated baseline records **exit 1** with its two `NOT CONVERGING` ladder verdicts — the honest record of the known-failing state, not a stale passing artifact.
- [x] 7.5 Run `cargo test -p deep_causality_cfd --release` and confirm no regression against the 813-pass baseline — **828 passed, 0 failed, 1 ignored** (+15 from the two new type test files; no pre-existing test lost)
- [x] 7.6 Run `make format && make fix` and confirm clippy is clean without new `#[allow]` suppressions — `cargo fmt` + `cargo clippy --all-targets` clean on both `deep_causality_cfd` and `avionics_examples` (0 warnings, 0 errors); **0 new `#[allow]` attributes** in the diff.
- [x] 7.7 Confirm the diff touches no file under `src/solvers/`, `src/navigation/` or `src/coordinate/` — the Phase 2 boundary, with `src/types/flow/mms.rs` the single permitted gate-logic exception — **confirmed, no files under those paths are modified.** The only `src/` changes are `types/evidence_class.rs` and `types/ladder_outcome.rs` (new), `types/mod.rs`, `lib.rs`, `types/flow/study/{verdict,gate_seq}.rs` (evidence-class plumbing), and `types/flow/mms.rs` (the permitted exception).
- [x] 7.8 Record any gate that now fails, with its triage against `openspec/notes/cfd_audit/ACTION-LIST.md`, so no bound was moved without evidence — **one harness now fails: `qtt_cylinder_verification`.** Triage: both new parameter ladders report `NOT CONVERGING` — η (0.128→0.008) gives C_d 17.39, 24.02, 26.25, 23.76, 21.40 (reverses direction, no η→0 limit) and mask smoothing (0.5→4 cells) gives 7.70→47.27 (6.1× on a numerical parameter). Both reproduce the audit's measured values exactly. Root cause is the audit's own finding: the Brinkman layer `√(ην) = 0.144·dx` is ~7× thinner than a cell and `η ≥ dx²/ν = 0.771` is violated 48×. **No bound was widened.** Per owner decision the gate is kept and the harness moved to the nightly cadence; routed to AUDIT-REPORT §9 Phase 2 item 10 and documented under `KNOWN-FAILING` in the workflow. All other harnesses pass.
