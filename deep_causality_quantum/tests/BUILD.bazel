load("@rules_rust//rust:defs.bzl", "rust_test_suite")

# =============================================================================
# error/* test suites
#
# The src tree is organized as src/error/, so the tests mirror it
# at tests/error/.
# =============================================================================

rust_test_suite(
    name = "error",
    srcs = glob([
        "error/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality_core",
        "//deep_causality_metric",
    ],
)

# =============================================================================
# kernels/* test suites
#
# The src tree is organized as src/kernels/, so the tests mirror it
# at tests/kernels/.
# =============================================================================

rust_test_suite(
    name = "kernels",
    srcs = glob([
        "kernels/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality_core",
        "//deep_causality_multivector",
        "//deep_causality_num",
        "//deep_causality_num_complex",
        "//deep_causality_tensor",
    ],
)

# =============================================================================
# types/* test suites
# =============================================================================

rust_test_suite(
    name = "types",
    srcs = glob([
        "types/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality_num_complex",
        "//deep_causality_tensor",
    ],
)

# =============================================================================
# qcm/* test suites — the verifiable QCM slice (freeze-integration + C₃).
# These exercise the engine freeze hook, so they depend on `deep_causality`.
# =============================================================================

rust_test_suite(
    name = "qcm",
    srcs = glob([
        "qcm/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality",
        "//deep_causality_core",
        "//deep_causality_num_complex",
        "//deep_causality_tensor",
    ],
)

# =============================================================================
# qpu/* test suites — the emergent seam behind the `qpu` feature.
# =============================================================================

rust_test_suite(
    name = "qpu",
    srcs = glob([
        "qpu/*_tests.rs",
    ]),
    crate_features = [
        "qpu",
    ],
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality_uncertain",
    ],
)

# =============================================================================
# verdict/* test suites — the orthomodular projection-lattice Verdict carrier.
# =============================================================================

rust_test_suite(
    name = "verdict",
    srcs = glob([
        "verdict/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_quantum",
        # Internal dependencies
        "//deep_causality_algebra",
        "//deep_causality_num_complex",
        "//deep_causality_tensor",
    ],
)
