load("@rules_rust//rust:defs.bzl", "rust_test_suite")

rust_test_suite(
    name = "extensions",
    srcs = glob([
        "extensions/*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_haft",
    ],
)

rust_test_suite(
    name = "tests",
    srcs = glob([
        "*_tests.rs",
    ]),
    tags = [
        "unit-test",
    ],
    visibility = ["//visibility:public"],
    deps = [
        # Crate to test
        "//deep_causality_haft",
    ],
)
