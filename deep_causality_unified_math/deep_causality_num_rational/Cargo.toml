[package]
name = "deep_causality_num_rational"
version = "0.1.3"
edition = { workspace = true }
rust-version = { workspace = true }
license = { workspace = true }

repository = "https://github.com/deepcausality/deep_causality.rs"
authors = ["Marvin Hansen <marvin.hansen@gmail.com>", ]
description = "Exact rational number type (the field of fractions of an integral domain) for deep_causality."
documentation = "https://docs.rs/deep_causality_num_rational"
categories = ["development-tools", "mathematics"]
keywords = ["rational", "fraction", "exact-arithmetic"]
# Exclude all bazel files as these conflict with Bazel workspace when vendored.
exclude = [
    "*.bazel",
    "*/*.bazel",
    "*.bazel.*",
    "BUILD",
    "BUILD.bazel",
    "MODULE.bazel",
    ".bazelignore",
    ".bazelrc",
    "tests/*",
    "tests/**/*",
    "papers/*",
]

[features]
default = ["std"]
std = ["deep_causality_num/std", "deep_causality_algebra/std"]
no-std = ["deep_causality_num/no-std", "deep_causality_algebra/no-std"]

[dependencies.deep_causality_num]
path = "../deep_causality_num"
version = "0.4"
default-features = false

[dependencies.deep_causality_algebra]
path = "../deep_causality_algebra"
version = "0.4"
default-features = false

[lints]
workspace = true
