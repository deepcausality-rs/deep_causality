load("@rules_rust//rust:defs.bzl", "rust_binary")

rust_binary(
    name = "tokio_example",
    srcs = glob(["src/**/*.rs"]),
    crate_root = "src/main.rs",
    visibility = ["//visibility:public"],
    deps = [
        "//deep_causality",
        "@crates//:tokio",
    ],
)
