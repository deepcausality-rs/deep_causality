load("@crates//:defs.bzl", "aliases", "all_crate_deps", "lint_config")
load("@rules_rs//rs:rust_binary.bzl", "rust_binary")

rust_binary(
    name = "propagating_effect_example",
    srcs = ["examples/propagating_effect.rs"],
    crate_root = "examples/propagating_effect.rs",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
        # Cargo `[[example]]` targets link dev-dependencies.
        normal_dev = True,
    ),
)

rust_binary(
    name = "propagating_effect_counterfactual_example",
    srcs = ["examples/propagating_effect_counterfactual.rs"],
    crate_root = "examples/propagating_effect_counterfactual.rs",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
        # Cargo `[[example]]` targets link dev-dependencies.
        normal_dev = True,
    ),
)

rust_binary(
    name = "propagating_process_example",
    srcs = ["examples/propagating_process.rs"],
    crate_root = "examples/propagating_process.rs",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
        # Cargo `[[example]]` targets link dev-dependencies.
        normal_dev = True,
    ),
)

rust_binary(
    name = "propagating_process_counterfactual",
    srcs = ["examples/propagating_process_counterfactual.rs"],
    crate_root = "examples/propagating_process_counterfactual.rs",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(
        normal = True,
        # Cargo `[[example]]` targets link dev-dependencies.
        normal_dev = True,
    ),
)
