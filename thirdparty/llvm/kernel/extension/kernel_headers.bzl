load("@bazel_features//:features.bzl", "bazel_features")
load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

def _kernel_headers_trampoline_repository_impl(repository_ctx):
    repository_ctx.template("BUILD.bazel", repository_ctx.attr._build_file)
    repository_ctx.file("linux_kernel_version_map.bzl", repository_ctx.attr.version_map)

_kernel_headers_trampoline_repository = repository_rule(
    implementation = _kernel_headers_trampoline_repository_impl,
    attrs = {
        "version_map": attr.string(mandatory = True),
        "_build_file": attr.label(
            allow_single_file = True,
            default = ":trampoline.BUILD.bazel",
        ),
    },
)

_SUPPORTED_ARCHS = [
    # "alpha",
    # "arc",
    # "arm",
    # "csky",
    # "loongarch",
    # "m68k",
    # "mips",
    # "openrisc",
    # "powerpc",
    "riscv",
    "s390",
    # "sh",
    # "sparc",
    "x86",
    "arm",
    "arm64",
]

def _version_key(version):
    return tuple([int(component) for component in version.split(".")])

def _major_minor(version):
    components = version.split(".")
    return "{}.{}".format(components[0], components[1])

def _linux_kernel_version_map(decoded_index):
    versions = {}
    for version in decoded_index.keys():
        major_minor = _major_minor(version)
        if major_minor not in versions or _version_key(version) > _version_key(versions[major_minor]):
            versions[major_minor] = version

    return versions

def _linux_kernel_version_map_content(decoded_index):
    versions = _linux_kernel_version_map(decoded_index)
    lines = [
        "# Generated by //kernel/extension:kernel_headers.bzl from kernel_headers_index.json.",
        "LINUX_KERNEL_VERSION_MAP = {",
    ]

    for major_minor in sorted(versions.keys(), reverse = True):
        lines.append('    "{}": "{}",'.format(major_minor, versions[major_minor]))

    lines.append("}")
    lines.append("")
    lines.append("LINUX_KERNEL_ARCHS_BY_VERSION = {")

    for version in sorted(decoded_index.keys(), key = _version_key, reverse = True):
        kernel_archs = sorted([
            kernel_arch
            for kernel_arch in decoded_index[version]
            if kernel_arch in _SUPPORTED_ARCHS
        ])
        lines.append('    "{}": [{}],'.format(
            version,
            ", ".join(['"{}"'.format(kernel_arch) for kernel_arch in kernel_archs]),
        ))

    lines.append("}")
    lines.append("")

    return "\n".join(lines)

def _kernel_headers_impl(module_ctx):
    """Kernel headers extension."""

    decoded_index = {}
    for mod in module_ctx.modules:
        for index_tag in mod.tags.index:
            file_path = module_ctx.path(index_tag.file)
            file_content = module_ctx.read(file_path)
            decoded_index = json.decode(file_content, default = None)

    for version, archs in decoded_index.items():
        for kernel_arch, index_entry in archs.items():
            if kernel_arch not in _SUPPORTED_ARCHS:
                continue
            repo = "linux_kernel_headers_%s.%s" % (kernel_arch, version)
            http_archive(
                name = repo,
                url = index_entry.get("url"),
                sha256 = index_entry.get("sha256"),
                strip_prefix = kernel_arch,
                build_file = ":kernel-headers.BUILD.bazel",
            )

    _kernel_headers_trampoline_repository(
        name = "kernel_headers",
        version_map = _linux_kernel_version_map_content(decoded_index),
    )

    repos = ["kernel_headers"]
    is_non_dev_dependency = module_ctx.root_module_has_non_dev_dependency
    root_direct_deps = list(repos) if is_non_dev_dependency else []
    root_direct_dev_deps = list(repos) if not is_non_dev_dependency else []

    metadata_kwargs = {}
    if bazel_features.external_deps.extension_metadata_has_reproducible:
        metadata_kwargs["reproducible"] = True

    return module_ctx.extension_metadata(
        root_module_direct_deps = root_direct_deps,
        root_module_direct_dev_deps = root_direct_dev_deps,
        **metadata_kwargs
    )

kernel_headers_index = tag_class(
    attrs = {
        "file": attr.label(
            allow_single_file = True,
            default = ":kernel_headers_index.json",
            mandatory = True,
        ),
    },
)

kernel_headers = module_extension(
    implementation = _kernel_headers_impl,
    tag_classes = {
        "index": kernel_headers_index,
    },
)
