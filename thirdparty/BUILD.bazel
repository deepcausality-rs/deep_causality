load("@rules_rust//crate_universe:defs.bzl", "crate", "crates_vendor")

crates_vendor(
    name = "crates_vendor",
    annotations = {},
    mode = "local",
    packages = {
        #
        # Proc macros dependencies
        "rusty-fork": crate.spec(
            default_features = True,
            package = "rusty-fork",
            version = "0.3",
        ),
        #
        # Regular dependencies
        # CXV and parquet are used in deep_causality_discovery for file loading
        "csv": crate.spec(
            default_features = False,
            package = "csv",
            version = "1.4",
        ),
        "parquet": crate.spec(
            default_features = False,
            package = "parquet",
            version = "57",
        ),
        "libm": crate.spec(
            default_features = True,
            package = "libm",
            version = "0.2",
        ),
        "tempfile": crate.spec(
            default_features = False,
            package = "tempfile",
            version = "3",
        ),
    },  # End packages
    repository_name = "deep_causality_vendored",
    tags = ["manual"],
)
